{
  "name": "vanilla-js-site",
  "version": "1.0.0",
  "description": "KTL static site",
  "scripts": {
    "start": "node index.js",
    "build": "cd project && npm run build",
    "dev": "cd project && npm run dev",
    "lint": "cd project && npm run lint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
